<nav class="navbar navbar-default" role="navigation">
  <div class="col-sm-8 col-sm-offset-2">
    <!-- <div class="container-fluid"> -->
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button id="right-menu" class="navbar-toggle" data-toggle="collapse"  href="#sidr-right">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>

        <!--<%= link_to "devocode", root_path, class: "navbar-brand" %>-->
        <%= link_to root_path do %>
          <%= image_tag("devocode-logo1.png", class: "img-responsive") %>
        <% end %>
      </div>

      <div id="sidr-right">
         <ul>
            <li><%= link_to "Process", courses_path %></li>
            <li><%= link_to "News", blogs_path %></li>
            <% if user_signed_in? %>
            <% else %>
              <li><%= link_to "Clients", user_session_path %></li>
            <% end %>
            <% if user_signed_in? %>
              <li><%= link_to "My Profile", edit_user_registration_path %></li>
              <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
            <% else %>
              <li><%= link_to "Contact", new_user_registration_path %></li>
            <% end %>
         </ul>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse">
        
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to "Process", courses_path %></li>
          <li><%= link_to "News", blogs_path %></li>
          <% if user_signed_in? %>
          <% else %>
            <li><%= link_to "Clients", user_session_path %></li>
          <% end %>
          <% if user_signed_in? %>
            <li><%= link_to "My Profile", edit_user_registration_path %></li>
            <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
          <% else %>
            <li><%= link_to "Contact", new_user_registration_path, class: "btn btn-sign" %></li>
          <% end %>
        </ul>
      </div><!-- /.navbar-collapse -->
    <!--</div> /.container-fluid -->
  </div>
</nav>